/**
 * Central mount point for sheets/screens that float above the tab content
 * (Penny sheet, pay sheet, ARC, onboarding, sign-in, etc). Extended by later
 * tasks (6-15); renders nothing yet.
 */
export function Overlays() {
  return null;
}
